apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-judge-api.fullname" . }}-test-connection"
  labels:
    {{- include "llm-judge-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-judge-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
